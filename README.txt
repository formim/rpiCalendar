README.txt

Usage:
  Run main.py from the rpiCalendar directory using Python 3.

Dependencies:
  See requirements.txt file. Use:
    $ pip install --user --requirements requirements.txt
  to install.

Output:
  Generates .ics file titled 'academic_calendar.ics', import this to your favorite
  calendar application.
